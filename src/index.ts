/**
 * Background plugin, node half.
 *
 * Deliberately empty. The feature is a pure browser-side visual; the Loader
 * imports this half only to mount the row, and nothing here contributes Host
 * behavior.
 */

/** Host plugin body — the background surface is composed entirely client-side. */
export function apply(): void {}
